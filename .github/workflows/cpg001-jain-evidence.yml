name: cpg001-jain-real-evidence-r4-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'R1-R4 are frozen as BLOCKED_SOURCE_ACQUISITION.'
          echo 'Automated publisher/BioStudies acquisition is retired.'
          echo 'Use CPG-001 Canonical Source Bind with manually acquired local publisher artifacts.'
