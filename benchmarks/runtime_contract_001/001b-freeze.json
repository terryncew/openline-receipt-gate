{
  "base_sha": "6c01bfcfe6f1226ec1483a0000817401cc937814",
  "claim_boundary": [
    "This tests transitive consequence discovery, not direct standing enforcement.",
    "It does not test sufficiency when a decision has multiple independent bases.",
    "It does not implement or import Claim Graph.",
    "The canonical A decision receipt is a fixture artifact, not a new product primitive.",
    "A FAIL earns the need for ancestry/closure machinery; it does not dictate one implementation."
  ],
  "constraints": {
    "claim_graph_or_dependency_remedy_forbidden": true,
    "deterministic_local_fixture": true,
    "fixture_or_source_guard_failure_is_red_ci": true,
    "no_new_product_code": true,
    "product_source_must_equal_base": true,
    "scientific_pass_or_fail_is_green_ci": true
  },
  "experiment": "RUNTIME-CONTRACT-001B",
  "fail": {
    "condition": "After X loses standing, A is directly blocked but B still executes under its unchanged direct standing while C also executes.",
    "verdict": "TRANSITIVE_CONSEQUENCE_NOT_DISCOVERED"
  },
  "fixed_sequence": [
    "Create canonical signed support X for exact decision A.",
    "Receiver admits X as ACTIVE for A.",
    "A executes through the current authorize/compiler/preflight path.",
    "Fixture issues canonical receiver-signed committed decision receipt A recording X's support hash as A's basis.",
    "Decision B uses exactly receipt A as its direct support artifact.",
    "Receiver admits receipt A as ACTIVE for B.",
    "Independent control C receives unrelated signed support and ACTIVE standing.",
    "B and C both execute before the standing change.",
    "Receiver admits REVOKE/INACTIVE successor for X -> A.",
    "Retry A; direct standing must block it.",
    "Retry B without any hand-written X -> B update.",
    "Retry unrelated C."
  ],
  "fixture_revision": {
    "claim_graph_or_dependency_logic_added": false,
    "reason": "Repair only current-runtime compatibility before any scientific outcome. Use the existing allowed action type inspect, and use a canonical signed receiver decision receipt for A because legacy ReceiptGate v0.1.1 receipts contain a float timestamp that standing canonicalization rejects.",
    "scientific_question_changed": false
  },
  "fixture_validity": [
    "A, B, and C execute before X loses standing.",
    "A's receiver-signed committed receipt records X's exact support hash as its basis.",
    "B's direct support artifact is exactly A's committed receipt.",
    "After X revocation, direct retry of A is blocked.",
    "After X revocation, unrelated C still executes.",
    "olp_gate source tree is byte-identical to the frozen base."
  ],
  "pass": {
    "condition": "After X loses standing, current runtime blocks/reopens B without an explicit X -> B update, while C remains unaffected.",
    "verdict": "TRANSITIVE_CONSEQUENCE_ALREADY_ENFORCED"
  },
  "prior_infrastructure_run": {
    "failure": "fixture used action_type apply_decision, which current MandateSpec rejects",
    "scientific_verdict_produced": false,
    "workflow_run_id": 33937059407
  },
  "question": "If support X justified decision A, and A's committed decision receipt later becomes the direct support for decision B, can the current OpenLine runtime discover that B is affected when X loses receiver-recognized standing, without being explicitly told that B depends on X?",
  "reason_001a_skipped": "STANDING-SEAM-001 already proves the direct case: a relevant receiver-admitted standing loss blocks the same unchanged action while an unrelated action survives.",
  "schema": "openline.runtime-contract-001b.freeze.v2",
  "status": "FROZEN_BEFORE_SCIENTIFIC_RUN"
}
