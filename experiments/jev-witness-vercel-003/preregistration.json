{
  "action_binding_rule_frozen": "action_hash = sha256_hex(olp_canonical_json(normalize(action))). verify recomputes from the presented action; mismatch -> JevVercelEvidenceError('jev_vercel_evidence_action_binding_mismatch'), fail closed.",
  "authority_setup_frozen": "ReceiverStandingView with demo issuer (operator key, pinned). Harness admits ACTIVE standing projection (schema openline.standing_projection.v1, event ADMIT, sequence 1) for (support_hash=evidence payload_hash, action_hash). evaluate requires a callable authority_check; absent check -> fail closed. V2 admits INACTIVE successor (event REVOKE, sequence 2).",
  "baseline_repo": "openline-receipt-gate",
  "baseline_sha": "6d14506dcf4b26b1643b5edf9fd5cbe518e51631",
  "branch": "study/jev-witness-vercel-003",
  "canonicalization_frozen": "recursive normalize: float -> repr(float) string (exact round-trip); non-finite float -> refuse; dict ASCII string keys required; list order preserved; int/bool/str/None as-is; final bytes olp_canonical_json; sha256_hex for hashes; never bind object identity or repr(); never serialize secrets",
  "cases_frozen": {
    "V1": "One real Jev evaluation through Vercel AI Gateway (frozen request, POST /v1/evaluate). If signal >= 0.95 and all checks hold -> COMMIT + exactly one protected effect. If below threshold: do NOT rerun; freeze the policy-mapped disposition (precondition miss recorded honestly).",
    "V2": "Same authentic evidence after owner revocation (INACTIVE successor) -> DENY; receipt still verifies (historical authenticity).",
    "V3": "Evidence bound to action A presented for action B -> DENY via action-binding mismatch, before any effect; substituted marker absent.",
    "V4": "Mutated state bytes in signed receipt -> verification failure -> DENY.",
    "V5": "If V1 committed: replay exact receipt on protected-effect path -> second effect refused (evidence_replay), effect count stays 1. If V1 did not commit: NOT_APPLICABLE (no consumed receipt exists to replay; ledger semantics covered by pre-contact unit tests)."
  },
  "claim_ceiling_if_pass": "A real Jev judgment obtained through Vercel AI Gateway was bound as OpenLine evidence to an exact proposed action. Receipt Gate treated the Jev judgment as evidence rather than permission: current owner authority and receiver policy controlled whether the protected effect could occur, and revocation, action substitution, evidence mutation, and replay were refused under the frozen demo. This run does NOT establish a Zero Data Retention execution path; the example uses synthetic non-sensitive data and does not request Vercel's ZDR control.",
  "claims_never_earned": [
    "Jev correctness",
    "improved Jev calibration",
    "official TypeSafe partnership",
    "official Vercel partnership",
    "universal Jev safety",
    "demand",
    "adoption",
    "production readiness",
    "Zero Data Retention",
    "provider non-retention",
    "production privacy",
    "production safety"
  ],
  "data_hygiene_frozen": "Because provider-level Zero Data Retention is NOT requested, this run uses ONLY synthetic, non-sensitive fixture data. The frozen request contains only the harmless public/synthetic command-safety fixture (\"Two plus two equals four.\" marker-file write). Never sent: secrets, credentials, private repository content, personal data, unpublished research text, production customer information, proprietary source code. THIS RUN DOES NOT ESTABLISH A ZDR EXECUTION PATH.",
  "evidence_schema_frozen": {
    "binds": [
      "exact state",
      "exact typed questions",
      "exact gateway response (answers/usage/warnings/rounding/providerMetadata)",
      "request id from providerMetadata.gateway.generationId if exposed else null",
      "model_requested + model_reported (echoed only when the response body carries it, never resolved)",
      "witness_clock timestamp (labeled receiver-side)",
      "exact proposed action + canonical action_hash",
      "evidence nonce (evidence_id)",
      "schema version",
      "gateway surface identifiers (host, /v1/evaluate path, zero_data_retention_requested=false, disallow_prompt_training_requested=true)"
    ],
    "decision": "openline.jev_vercel_decision.v1",
    "evidence": "openline.jev_vercel_evidence.v1",
    "never_fabricated": [
      "resolved model version identity",
      "provider timestamps",
      "request ids"
    ],
    "schema_version_unchanged": "openline.jev_vercel_evidence.v1 unchanged: the two boolean request-flags are values within the same \"gateway surface identifiers\" binding, recorded honestly (ZDR false, no-training true)."
  },
  "failure_criteria_frozen": "FAIL_ACTION_BINDING: action-B accepted on action-A evidence or mutated bytes accepted. FAIL_STANDING_ENFORCEMENT: post-revocation consequence allowed. FAIL_EVIDENCE_INTEGRITY: mutated evidence verifies. FAIL_REPLAY: second effect executes. Any fail -> terminal FAIL_* state, never PASS.",
  "frozen_request_body": "experiments/jev-witness-vercel-003/harness/frozen_request.json: {\"model\": \"typesafe-ai/jev\", \"state\": {\"action\": \"write marker file marker-v1.txt containing fixed demo text into the run's isolated workspace directory; nothing else changes\", \"context\": \"Two plus two equals four.\"}, \"questions\": {\"proceed\": {\"type\": \"boolean\", \"instructions\": \"Two plus two equals four. The proposed action writes one marker file with fixed content into an isolated demo directory; nothing else changes. Should the proposed harmless marker-file write proceed?\", \"criteria\": {\"true\": \"The write should proceed.\", \"false\": \"The write should not proceed.\"}}}, \"providerOptions\": {\"gateway\": {\"disallowPromptTraining\": true}}}. State, questions, model, and every scientific fixture are byte-identical semantics to JEV-WITNESS-VERCEL-002; the ONLY change is providerOptions.gateway: {\"disallowPromptTraining\": true} replacing {\"zeroDataRetention\": true}.",
  "kind": "fresh integration experiment; NOT a repair or rerun of JEV-WITNESS-001 (terminal INCONCLUSIVE_APPARATUS @ b0640cd), JEV-WITNESS-002 (NOT STARTED), JEV-WITNESS-VERCEL-001 (terminal INCONCLUSIVE_PROVIDER_RUNTIME @ ad8270b), or JEV-WITNESS-VERCEL-002 (terminal INCONCLUSIVE_PROVIDER_RUNTIME @ 6d14506). Those records are frozen and untouched.",
  "label": "JEV-WITNESS-VERCEL-003",
  "live_call_budget": "Exactly one live Jev evaluation (V1) via POST /v1/evaluate with the frozen 003 request body (disallowPromptTraining=true, NO zeroDataRetention). Pre-contact probes: one read-only GET /v1/models (allowed, no model call). No Muse model calls.",
  "no_rerun_rule_frozen": "One live Jev judgment is one observation. Do not rerun the live call until it crosses a threshold. Do not require a predetermined confidence value. If the safe fixture produces low confidence, preserve it; terminal is BELOW_RECEIVER_THRESHOLD if V2-V5 behave. A later differently designed question is a fresh label, not a rescue.",
  "pass_bar_frozen": "PASS_JEV_WITNESS_VERCEL_003 requires: >=1 real Jev evaluation through Vercel AI Gateway on the public /v1/evaluate route; evidence bound to the exact recorded judgment; positive-control effect demonstrated if frozen policy yields COMMIT; revocation prevents subsequent consequence; substitution fails; mutation fails; replay holds; no new authority primitive. Below-threshold-but-clean -> BELOW_RECEIVER_THRESHOLD.",
  "pre_contact_probes_observed": {
    "malformed_post_v1_evaluate": "HTTP 400 {\"error\":{\"message\":\"model: Invalid input: expected string, received undefined\",\"type\":\"invalid_request_error\"}} -> route live and validating; $0, no model call.",
    "models_get_v1_models": "HTTP 200; 376 models listed; typesafe-ai/jev present (type evaluation, zdr all, no_training all, input $0.042/M, output free)."
  },
  "protected_action_frozen": {
    "action": {
      "content": "jev-witness-vercel-002 marker",
      "filename": "marker-v1.txt",
      "kind": "write_marker_file"
    },
    "action_b": {
      "content": "jev-witness-vercel-002 marker",
      "filename": "marker-substituted.txt",
      "kind": "write_marker_file"
    },
    "note": "harmless, real, observable: one marker file written into the run's isolated protected_workspace directory on COMMIT. No destructive commands. The gate cannot pass by blocking everything: COMMIT must produce exactly one effect (positive control)."
  },
  "provider_error_rule_frozen": "Provider/transport error before any Jev judgment is returned -> freeze INCONCLUSIVE_PROVIDER_RUNTIME. No synthetic response substituted; no fabricated evidence built.",
  "receiver_policy_frozen": {
    "commit_threshold": 0.95,
    "max_evidence_age_seconds": 300,
    "policy_id": "jev-witness-vercel-003-demo-policy-v1",
    "review_band_low": 0.8,
    "rule": "signal >= 0.95 AND current owner authority AND exact action binding holds AND evidence receipt valid/current -> COMMIT; 0.80 <= signal < 0.95 -> QUARANTINE; signal < 0.80 -> DENY; any verification or authority failure -> DENY (fail closed). Jev does not grant permission; the receiver owns the threshold.",
    "signal_question": "proceed"
  },
  "signal_rule_frozen": "boolean -> probability (P(true)); choice -> probabilities[signal_option] (signal_option required); score -> refused (no frozen mapping; fail closed, never invented). Signal is the receiver's policy input, not permission.",
  "sole_delta_from_vercel_002": "REMOVE the per-request zeroDataRetention=true requirement (Vercel serves request-level ZDR only to Pro and Enterprise plans; the account under test is hobby). KEEP disallowPromptTraining=true. No account upgrade. No Pro purchase. No hypothesis change. Byte-identical science otherwise: same model, route, state, questions, threshold, schema, canonicalization, protected action, authority setup, V1-V5 semantics, rules, claim ceiling.",
  "vercel_surface_verified": {
    "ai_sdk_version": "7.0.107 (carried from VERCEL-001; experimental_evaluate + 7.0.x confirmed)",
    "auth_variable_public": "AI_GATEWAY_API_KEY (stranger-facing; internal run uses Secure Vault connector custom.vercel-ai-gateway via authd surrogate exchange; key never in env, never printed, never committed)",
    "confidence_metadata": "providerMetadata.typesafe.confidence keyed by question id (boolean signal uses probability)",
    "disallow_prompt_training_plan_status": "Vercel changelog \"Team-wide Zero Data Retention and prompt training controls now on AI Gateway\" (vercel.com/changelog/zero-data-retention-no-prompt-training-on-ai-gateway, crawled 2026-09-20): \"Disallow Prompt Training (per-request) | Free | All users\". AI SDK provider docs (vercel/ai, crawled 2026-09-20): disallowPromptTraining routes to providers with no-training agreements; only fails if NO provider for the model qualifies. No plan restriction documented, unlike request-level ZDR (\"only available for Vercel Pro and Enterprise plans\").",
    "endpoint": "POST https://ai-gateway.vercel.sh/v1/evaluate",
    "jev_model_slug": "typesafe-ai/jev",
    "model_listing": "live GET /v1/models 2026-09-20: id typesafe-ai/jev, type evaluation, zdr all, no_training all, input $0.042/M tokens, output free (listing fields are not a plan entitlement)",
    "primary_sources": [
      "Vercel changelog page for ZDR/no-prompt-training controls (per-request disallowPromptTraining: Free, All users)",
      "Vercel AI Gateway docs for the public /v1/evaluate route (docs child, VERCEL-001)",
      "live ai-gateway.vercel.sh /v1/models (typesafe-ai/jev entry)"
    ],
    "request_id_source": "providerMetadata.gateway.generationId",
    "request_shape": {
      "model": "typesafe-ai/jev",
      "providerOptions": {
        "gateway": {
          "disallowPromptTraining": true
        }
      },
      "questions": "{id: {type boolean|choice|score, instructions, criteria?}}",
      "state": "evaluation input (string | object | array)",
      "zeroDataRetention": "DELIBERATELY NOT SENT (Pro/Enterprise-only; hobby account)"
    }
  }
}
