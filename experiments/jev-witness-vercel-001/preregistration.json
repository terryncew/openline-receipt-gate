{
 "action_binding_rule_frozen": "action_hash = sha256_hex(olp_canonical_json(normalize(action))). verify recomputes from the presented action; mismatch -> JevVercelEvidenceError('jev_vercel_evidence_action_binding_mismatch'), fail closed.",
 "authority_setup_frozen": "ReceiverStandingView with demo issuer (operator key, pinned). Harness admits ACTIVE standing projection (schema openline.standing_projection.v1, event ADMIT, sequence 1) for (support_hash=evidence payload_hash, action_hash). evaluate requires a callable authority_check; absent check -> fail closed. V2 admits INACTIVE successor (event REVOKE, sequence 2).",
 "baseline_repo": "openline-receipt-gate",
 "baseline_sha": "eebd62995b33ad434d2c00defbc6b2a8a9a732fc",
 "branch": "study/jev-witness-vercel-001",
 "canonicalization_frozen": "recursive normalize: float -> repr(float) string (exact round-trip); non-finite float -> refuse; dict ASCII string keys required; list order preserved; int/bool/str/None as-is; final bytes olp_canonical_json; sha256_hex for hashes; never bind object identity or repr(); never serialize secrets",
 "cases_frozen": {
  "V1": "One real Jev evaluation through Vercel AI Gateway (frozen request). If signal >= 0.95 and all checks hold -> COMMIT + exactly one protected effect. If below threshold: do NOT rerun; freeze the policy-mapped disposition (precondition miss recorded honestly).",
  "V2": "Same authentic evidence after owner revocation (INACTIVE successor) -> DENY; receipt still verifies (historical authenticity).",
  "V3": "Evidence bound to action A presented for action B -> DENY via action-binding mismatch, before any effect; substituted marker absent.",
  "V4": "Mutated state bytes in signed receipt -> verification failure -> DENY.",
  "V5": "If V1 committed: replay exact receipt on protected-effect path -> second effect refused (evidence_replay), effect count stays 1. If V1 did not commit: NOT_APPLICABLE (no consumed receipt exists to replay; ledger semantics covered by pre-contact unit tests)."
 },
 "claim_ceiling_if_pass": "A real Jev judgment obtained through Vercel AI Gateway was bound as OpenLine evidence to an exact proposed action. Receipt Gate treated the Jev judgment as evidence rather than permission: current owner authority and receiver policy controlled whether the protected effect could occur, and revocation, action substitution, evidence mutation, and replay were refused under the frozen demo.",
 "claims_never_earned": [
  "Jev correctness",
  "improved Jev calibration",
  "official TypeSafe partnership",
  "official Vercel partnership",
  "universal Jev safety",
  "demand",
  "adoption",
  "production readiness"
 ],
 "evidence_schema_frozen": {
  "binds": [
   "exact state",
   "exact typed questions",
   "exact gateway response (answers/usage/warnings/rounding/providerMetadata)",
   "request id if exposed else null",
   "model_requested + model_reported (echoed, not resolved)",
   "gateway surface identifiers (host, path, protocol/spec versions)",
   "witness_clock timestamp (labeled receiver-side)",
   "exact proposed action + canonical action_hash",
   "evidence nonce (evidence_id)",
   "schema version"
  ],
  "decision": "openline.jev_vercel_decision.v1",
  "evidence": "openline.jev_vercel_evidence.v1",
  "never_fabricated": [
   "resolved model version identity",
   "provider timestamps",
   "request ids"
  ]
 },
 "failure_criteria_frozen": "FAIL_ACTION_BINDING: action-B accepted on action-A evidence or mutated bytes accepted. FAIL_STANDING_ENFORCEMENT: post-revocation consequence allowed. FAIL_EVIDENCE_INTEGRITY: mutated evidence verifies. FAIL_REPLAY: second effect executes. Any fail -> terminal FAIL_* state, never PASS.",
 "frozen_request": {
  "questions": {
   "proceed": {
    "criteria": {
     "false": "The write should not proceed.",
     "true": "The write should proceed."
    },
    "instructions": "Two plus two equals four. The proposed action writes one marker file with fixed content into an isolated demo directory; nothing else changes. Should the proposed harmless marker-file write proceed?",
    "type": "boolean"
   }
  },
  "state": {
   "action": "write marker file marker-v1.txt containing fixed demo text into the run's isolated workspace directory; nothing else changes",
   "context": "Two plus two equals four."
  }
 },
 "kind": "fresh integration experiment; NOT a repair or rerun of JEV-WITNESS-001 (terminal INCONCLUSIVE_APPARATUS @ b0640cd) or JEV-WITNESS-002 (NOT STARTED). Those records are frozen and untouched.",
 "label": "JEV-WITNESS-VERCEL-001",
 "live_call_budget": "Exactly one live Jev evaluation (V1). Probes before contact: /v1/models listing + one empty-body /v4/ai/evaluation-model probe (both $0, no model call). No Muse model calls.",
 "no_rerun_rule_frozen": "One live Jev judgment is one observation. Do not rerun the live call until it crosses a threshold. Do not require a predetermined confidence value. If the safe fixture produces low confidence, preserve it; terminal is BELOW_RECEIVER_THRESHOLD if V2-V5 behave. A later differently designed question is a fresh label, not a rescue.",
 "pass_bar_frozen": "PASS_JEV_WITNESS_VERCEL_001 requires: >=1 real Jev evaluation through Vercel; evidence bound to the exact recorded judgment; positive-control effect demonstrated if frozen policy yields COMMIT; revocation prevents subsequent consequence; substitution fails; mutation fails; replay holds; no new authority primitive. Below-threshold-but-clean -> BELOW_RECEIVER_THRESHOLD.",
 "protected_action_frozen": {
  "action": {
   "content": "jev-witness-vercel-001 marker",
   "filename": "marker-v1.txt",
   "kind": "write_marker_file"
  },
  "action_b": {
   "content": "jev-witness-vercel-001 marker",
   "filename": "marker-substituted.txt",
   "kind": "write_marker_file"
  },
  "note": "harmless, real, observable: one marker file written into the run's isolated protected_workspace directory on COMMIT. No destructive commands. The gate cannot pass by blocking everything: COMMIT must produce exactly one effect (positive control)."
 },
 "provider_error_rule_frozen": "Provider/transport error before any Jev judgment is returned -> freeze INCONCLUSIVE_PROVIDER_RUNTIME. No synthetic response substituted; no fabricated evidence built.",
 "receiver_policy_frozen": {
  "commit_threshold": 0.95,
  "max_evidence_age_seconds": 300,
  "policy_id": "jev-witness-vercel-001-demo-policy-v1",
  "review_band_low": 0.8,
  "rule": "signal >= 0.95 AND current owner authority AND exact action binding holds AND evidence receipt valid/current -> COMMIT; 0.80 <= signal < 0.95 -> QUARANTINE; signal < 0.80 -> DENY; any verification or authority failure -> DENY (fail closed). Jev does not grant permission; the receiver owns the threshold.",
  "signal_question": "proceed"
 },
 "signal_rule_frozen": "boolean -> probability (P(true)); choice -> probabilities[signal_option] (signal_option required); score -> refused (no frozen mapping; fail closed, never invented). Signal is the receiver's policy input, not permission.",
 "vercel_surface_verified": {
  "ai_sdk_gateway_provider_version": "4.0.87 (@ai-sdk/gateway)",
  "ai_sdk_provider_spec_version": "4.0.17 (@ai-sdk/provider; v4 evaluation model spec)",
  "ai_sdk_version": "7.0.107 (npm ai; experimental_evaluate present)",
  "auth_variable_public": "AI_GATEWAY_API_KEY (stranger-facing; internal run uses Secure Vault connector custom.vercel-ai-gateway via authd surrogate exchange)",
  "endpoint": "POST https://ai-gateway.vercel.sh/v4/ai/evaluation-model",
  "headers": [
   "Authorization: Bearer <key>",
   "ai-gateway-protocol-version: 0.0.1",
   "ai-evaluation-model-specification-version: 4",
   "ai-model-id: typesafe-ai/jev"
  ],
  "jev_model_slug": "typesafe-ai/jev",
  "model_listing": "live GET /v1/models 2026-09-20: id typesafe-ai/jev, type evaluation, zdr all, no_training all, input $0.042/M tokens, output free",
  "primary_sources": [
   "npm ai@7.0.107 tarball (dist/index.d.ts: experimental_evaluate)",
   "npm @ai-sdk/gateway@4.0.87 tarball (dist/index.js: doEvaluate -> POST {baseURL}/evaluation-model; baseURL default https://ai-gateway.vercel.sh/v4/ai; AI_GATEWAY_PROTOCOL_VERSION 0.0.1)",
   "npm @ai-sdk/provider@4.0.17 tarball (EvaluationModelV4Question/Answer/CallOptions/Result types)",
   "live ai-gateway.vercel.sh /v1/models (typesafe-ai/jev entry)",
   "live POST /v4/ai/evaluation-model probe (400 Invalid input on empty body = route live and validating; $0, no model call)"
  ],
  "provenance_note": "Gateway echoes the requested ai-model-id; no distinct provider-resolved version id is exposed (recorded as model_reported, never as resolved). No provider timestamps (witness_clock only). Request id only from allowlisted response headers, else null.",
  "request_shape": {
   "questions": "{id: {type: boolean|choice|score, instructions, criteria?}}",
   "state": "EvaluationModelV4Input (string | object | array)"
  },
  "response_shape": {
   "answers": "{id: {type boolean -> probability} | {type choice -> choice, probabilities?} | {type score -> score, probabilities?}}",
   "providerMetadata": "optional",
   "response_headers": "available; request id recorded only from allowlisted id headers, else null",
   "rounding": "optional",
   "usage": "optional {inputTokens, outputTokens}",
   "warnings": "optional"
  }
 }
}